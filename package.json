{
  "name": "open-dynamic-workflows-plugin",
  "version": "0.2.0",
  "private": true,
  "description": "Dynamic workflow orchestration for Cursor, Grok Build, Codex, and ZCode. Dev dependencies only; the shipped dist/mcp/server.js is self-contained.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "build": "node scripts/build.mjs",
    "smoke": "node scripts/smoke.mjs",
    "test:host": "node --experimental-strip-types --test src/mcp/host.test.ts",
    "typecheck": "tsc --noEmit",
    "verify": "npm run build && npm run typecheck && npm run test:host && npm run smoke"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.3"
  }
}
