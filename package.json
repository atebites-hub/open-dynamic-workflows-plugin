{
  "name": "open-dynamic-workflows-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Dynamic workflow orchestration for ZCode — a zcode plugin packaging the Open Dynamic Workflows runtime. Dev dependencies only; the shipped dist/mcp/server.js is self-contained.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "build": "node scripts/build.mjs",
    "smoke": "node scripts/smoke.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.8.3"
  }
}
